const initial = {
  favs: [],
  current: "redux el sallıyor",
  error: null,
  loading: true,
};

export default function reducer(state = initial, action) {
  switch (action.type) {
    default:
      return state;
  }
}
